# CAD

`Kameraslider.step.zip` enthält den STEP-Export `Kameraslider.step`.

Der Export wurde auf eingebettete Namen, E-Mail-Adressen und lokale Dateipfade
geprüft; die entsprechenden STEP-Kopfzeilen sind leer. Die ZIP-Datei reduziert
den etwa 53 MB großen STEP-Export auf ungefähr 10 MB.
